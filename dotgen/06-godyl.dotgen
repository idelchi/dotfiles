values:
  SUDO: '{{ if eq .OS "linux" }}sudo {{ end }}'

---
commands:
  - name: godyld
    doc: download and install godyl
    cmd: |
      curl -sSL https://raw.githubusercontent.com/idelchi/godyl/refs/heads/main/install.sh | {{ .SUDO }}sh -s --

  - name: godylu
    doc: update godyl to the latest version
    cmd: "{{ .SUDO }}godyl update --pre --force"

  - name: godyla
    doc: install/sync all tools from godyl dump
    cmd: godyl dump tools -e | {{ .SUDO }}godyl i -

  - name: godyls
    doc: install/sync all tools from the tools file
    cmd: "{{ .SUDO }}godyl i"

  - name: godyle
    doc: install/sync all existing tools
    cmd: "{{ .SUDO }}godyl i --strategy=existing"

  - name: godyli
    doc: install/sync all idelchi tools from godyl dump
    cmd: godyl dump tools -e | {{ .SUDO }}godyl i --tags "idelchi/*" --pre --strategy=force -

  - name: godylgit
    doc: force install all gitlab related tools from godyl dump
    cmd: "{{ .SUDO }}godyl -j=2 -d ${GODYL_BASE_DIR}/defaults.yml i --strategy force ${GODYL_BASE_DIR}/gitlab.yml"
