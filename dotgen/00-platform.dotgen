values:
  deps:
    # Forces a rerun if any tool is changed
    tools:
      dotgen: "{{ which "dotgen" | size }}"

---

vars:
  OS: "{{ .OS }}"
  ARCH: "{{ .ARCHITECTURE }}"
  PLATFORM: "{{ .PLATFORM }}"

env:
  SHELL: "{{ .SHELL }}"
