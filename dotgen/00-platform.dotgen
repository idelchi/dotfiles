values:
  # Forces a rerun if any tool is added/removed
  deps:
    dotgen: "{{ which "dotgen" | size }}"

---

vars:
  OS: "{{ .OS }}"
  ARCH: "{{ .ARCHITECTURE }}"
  PLATFORM: "{{ .PLATFORM }}"
  SHELL: "{{ which .SHELL }}"
